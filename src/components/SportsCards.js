import React from 'react'

export default class SportsCards extends React.Component {
    render() {
        if (!this.props.show) {
            return null;
        } else {
        return(
            <div></div>
        )}
    }
}